//_________________Punlic_Define___________________________

	#ifdef EOF
	#else
		#define EOF -1
	#endif

	#ifdef NULL
	#else
		#define NULL 0
	#endif

	#ifdef SEEK_SET
	#else
		#define SEEK_SET 0
	#endif

	#ifdef SEEK_CUR
	#else
		#define SEEK_CUR 1
	#endif

	#ifdef SEEK_END
	#else
		#define SEEK_END 2
	#endif

